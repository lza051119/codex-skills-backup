interface:
  display_name: "Code Review"
  short_description: "Review pinned code changes across risk axes"
  default_prompt: "Use $code-review to review this PR or worktree against its repository-native authorities and report verified findings only."
